```cs
if (buff.EffectPrefabName!="" && pl != null)
{
    Transform eft = pl.transform.Find(buff.Id.ToString());
    if(eft != null)
    {
        eft.name += "(Destroy)";
        UnityEngine.Object.Destroy(eft.gameObject);
    }  
 }
```
发现同帧有多次创造buff删除buff的情况。

而创造的时候如果不改名。
```cs
if (pl.transform.Find(buff.Id.ToString())==null)
```

会判定我们还存在着这个特效，就没有创造出来。

原来删除前先改名是对的。
