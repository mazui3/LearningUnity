在mainPanel和interactPanel都没找到游戏机制的东西，搜到了是分开的一个系统。

点点乐使用Q frame + 状态机做的，Q frame会把交互层和功能层分开，then which make sense.\

一个system应该是在global一切开始时init。
```cs
RegisterSystem(new TapyFunSystem());
```

在开始游戏后，让状态机自己开始跑第一个State。
```cs
Global.Interface.GetSystem<ASMRSystem>().Start(param);
```

在System下注册状态机。
```cs
private FSM<TapyFunPlayState> _fsm { get; set; }//状态机
public TapyFunSystemRuntimeData Data;//运行时的数据
public TapyFunDataBase DataBase;//关卡数据
private AssetHandle _handle;//这个得再开一篇来讲.jpg
```
这是system存放的东西。

函数。
```cs
public bool IsPlaying
{
    get => _fsm.CurrentState is not (SettlementStage or IdleStage or FailStage);
}

//觉得这样很干净
protected override void OnInit()
{
    ClearData();
    BuildFsm();
    _handle = AssetsManager.Instance.LoadHandle<TapyFunDataBase>("TapyFunDatabase", out DataBase);
}

//可能有不同系数的重载
public void Start()
{
    ClearData();
    //再设定系数
    _fsm.ChangeState(TapyFunPlayState.Start);
}
```
