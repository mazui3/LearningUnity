在mainPanel和interactPanel都没找到游戏机制的东西，搜到了是分开的一个系统。

点点乐使用Q frame + 状态机做的，Q frame会把交互层和功能层分开，then which make sense.\

一个system应该是在global一切开始时init。
```cs
RegisterSystem(new TapyFunSystem());
```

在开始游戏后，让状态机自己开始跑第一个State。
```cs
Global.Interface.GetSystem<ASMRSystem>().Start(param);
```

在System下注册状态机。
```cs
private FSM<TapyFunPlayState> _fsm { get; set; }
public TapyFunSystemRuntimeData Data;
public TapyFunDataBase DataBase;
private AssetHandle _handle;
```
这是system存放的东西。
