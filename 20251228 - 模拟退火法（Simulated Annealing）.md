模拟退火法（Simulated Annealing, SA）模仿物理学中金属锻造的过程。
它是为了解决优化问题中最头疼的一个坑：“局部最优解陷阱”。

---

### 1. 核心痛点：为什么“只往高处走”会失败？

想象你在黑夜中爬山，目标是找到**最高峰（全局最优解）**。
如果你只用最简单的策略——“只要下一步比现在高，我就往哪走”，你很可能会爬到一个**小山丘（局部最优解）**的顶端。一旦到了那里，你会发现四周都是下坡，于是你以为自己登顶了。

---

### 2. 灵感来源：打铁的艺术

在冶金学中，要把金属变得更坚韧，工人会先将其加热到高温，然后**缓慢降温**。

* **高温时：** 原子的能量很高，可以剧烈跳动，打破原本混乱的排列。
* **降温时：** 原子会逐渐寻找能量最低、最稳定的位置。
* **缓慢是关键：** 如果降温太快（淬火），原子来不及调整，金属会变得很脆。

---

### 3. 模拟退火是怎么工作的？

算法模拟了这个“从动荡到稳定”的过程。它最精妙的地方在于：**它允许“退步”**。

它的决策规则非常人性化：

1. **如果新状态更好：** 100% 接受它，直接跳过去。
2. **如果新状态更差：** 它**不会直接拒绝**，而是以一定的**概率**接受它。
* 这个概率由“温度”决定。**温度越高，接受“退步”的可能性就越大。**

---

### 4. 算法的节奏：由乱到治

* **初始（高温）：** 算法像个“醉汉”，到处乱跳。即使是很差的一步它也愿意尝试，目的是为了**探索**整个地形。
* **中期（降温）：** 随着温度下降，它变得越来越谨慎，接受“退步”的概率变小。
* **后期（低温）：** 算法变得非常保守，只往更好的方向走，最终在最高峰附近**稳定**下来。

---

### 5. 常见对比

| 特性 | 爬山算法 (Hill Climbing) | 模拟退火算法 (SA) |
| --- | --- | --- |
| **策略** | 贪心，只进不退 | 早期允许退步，后期逐渐稳定 |
| **优点** | 速度极快 | **能跳出局部最优解** |
| **风险** | 容易目光短浅，死在半路 | 如果降温太快，效果就不理想 |
| **结果** | 找到最近的山头 | 更有可能找到真正的最高峰 |

---

### 总结

模拟退火法是一种**用概率换取全局视角**的策略。它告诉我们：有时候为了长远的目标，接受暂时的退步（Loss）反而是通往成功的必经之路。

在模拟退火法中，“温度”其实是一个**人为定义的参数**，它的变化并不取决于物理距离，而是取决于**算法运行的进度（迭代次数）**。

你可以把“温度”理解为算法的 **“狂躁指数”** 或 **“允许出错的额度”** 。

---

### 1. 温度是怎么决定的？（冷却计划）

温度通常是根据程序员预先设定好的**“冷却计划”（Cooling Schedule）**来自动变化的。这套计划包含四个要素：

#### ① 初始温度 ( $T_{start}$ )

一开始我们会设一个很高的值（比如 1000 或 10000）。

* **决定标准：** 确保在开始阶段，几乎所有的“坏尝试”都有很高的概率被接受，这样才能充分探索地图。

#### ② 降温公式（它是怎么变低的？）

最常见的方法是**等比降温**。每完成一轮计算，就把温度乘以一个系数 （通常在 0.8 到 0.99 之间）：

$$T_{new} = T_{old} \times \alpha$$

* **为什么用乘法？** 这样温度在前期下降很快，后期下降很慢，给算法留出时间在最后阶段做细微的精修。

#### ③ 每一度停留多久（等温过程）

算法并不会每动一步就降温。通常会在同一个温度下尝试 $L$ 次（比如交换 100 次路线），这个 $L$ 被称为**马尔可夫链长度**。

* 这模拟了物理上“热平衡”的过程，确保在这个热度下已经把能试的路都试得差不多了。

#### ④ 停止温度 ( $T_{end}$ )

当温度降到一个极低的值（比如 0.01）或者算法在很长时间内路程都没有再缩短时，程序就会关机，认为已经找到了最优解。

---

### 2. 它怎么知道“接受坏方案”的概率？

这是模拟退火法最核心的数学美感所在。它使用了一个叫做 **Metropolis 准则** 的公式：

如果新方案比旧方案差，它接受这个坏方案的概率 $P$ 是：
$$P = e^{-\frac{\Delta L}{T}}$$

* $\Delta L$ ：路程增加了多少（坏了多少）。
* $T$ ：当前温度。
* $e$ ：自然常数。

**这个公式妙在哪里？**

1. **温度 $T$ 越高，$P$ 越接近 1：** 高温时，哪怕方案很烂，我也愿意去试。
2. **温度 $T$ 越低，$P$ 越接近 0：** 低温时，只要方案变差一点点，我就极度排斥。
3. **方案越烂（$\Delta L$ 越大），$P$ 越小：** 即便在高温期，如果你这个方案烂得离谱，我也没那么容易接受。

---

### 3. 总结：它按什么决定降温？

它不按路程，而是按 **“时间步（迭代次数）”** 。

* **它是计时的：** “我已经跑了 5000 步了，按计划我现在该进入‘中年期’（中温），不能再乱搞了，要开始收敛了。”
* **它是不看终点的：** 算法其实并不知道真正的最短路程是多少，它只是机械地执行冷却计划。如果冷却太快，它会“淬火”失败，困在小山丘；如果冷却太慢，它会浪费大量计算资源。

