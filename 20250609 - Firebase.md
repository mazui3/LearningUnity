google firebase

https://firebase.google.com/docs/projects/learn-more

\
今天修改新版本firebase的时候报了一个错\
Unable to load DLL 'FirebaseCppApp-10_2_0'. on android build
>https://stackoverflow.com/questions/74812704/unity-dllnotfoundexception-unable-to-load-dll-firebasecppapp-10-2-0-on-and

注意firebase里的内容都是同个version就行。

\
很草，今天运营说没收到firebase打点，但默认的event（首次进入）这种能收到，一看firebase相关的内容在码内给注释掉了。\
```cs
//可以写在一个什么sdk处理的boxing中

//eName - string
if (!Application.isEditor && mIsFireBaseInitSuccess)
{
    Firebase.Analytics.FirebaseAnalytics.LogEvent(eName);
}
```
我们在firebase init时定义好mIsFireBaseInitSuccess的global之后就行,很straight forward.

```cs
//班码把它写进main camera里了，在main camera启动时处理。
private void InitFirebase()
{
  if (isInitFireBase)
  {
      return;
  }
  isInitFireBase = true;
  try
  {
    FirebaseApp.CheckAndFixDependenciesAsync().ContinueWith(task => {
        var dependencyStatus = task.Result;
        if (dependencyStatus == DependencyStatus.Available)
        {
            FirebaseApp app = FirebaseApp.DefaultInstance;
            Firebase.Analytics.FirebaseAnalytics.SetAnalyticsCollectionEnabled(true);
            Firebase.Analytics.FirebaseAnalytics.LogEvent(Firebase.Analytics.FirebaseAnalytics.EventLogin);
            Global.mIsFireBaseInitSuccess = true;
        }
    });             
  }
  catch (Exception e){ }
}
```
