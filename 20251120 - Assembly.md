看游戏开始时，一般挂一个Init或者GameManager，专门用于启动（&处理）游戏的空gameObject。\
老项目写在了MainCamera里，我觉得不是很好但是有道理（在说什么）。

这个Init写的挺干净
```cs
private void Start()
{
  DontDestroyOnLoad(gameObject);

  AppDomain.CurrentDomain.UnhandledException += (sender, e) => { Log.Error(e.ExceptionObject.ToString()); };
  Game.AddSingleton<MainThreadSynchronizationContext>();

  //这里的singleton是游戏必要模块
  //EventSystem
  //TimeInfo
  //ObjectPool
  //IDGenerator
  //CoroutineLockComponent
  Game.AddSingleton<theRequireModule>();
  
  Task.ExceptionHandler += Log.Error;

  StartGame();

  Thread.CurrentThread.CurrentCulture = new System.Globalization.CultureInfo("en-US");
  LocalizationManager.Init();//语言
  LoadConfigData();//读表

  //这里的init大多是给插件，打点用的，广告用的
  InitRequireModule();

  AudioManager.m_MusicPlayer = Camera.main.GetComponent<AudioSource>();
  Application.targetFrameRate = 60;
  QuestWebTime();//获取Web服务器时间
}
```

里面的一些细节之后再看，俺觉得很神秘的是，它的StartGame()，不是真正的StartGame。\
它写的是
```cs
Game.AddSingleton<CodeLoader>().Start();
```

```cs
public class CodeLoader: Singleton<CodeLoader>
{
  private Assembly model;

  public void Start()
  {
      //前略
      IStaticMethod start = new StaticMethod(this.model, "ET.Entry", "Start");
      start.Run();
  }
}
```

这个model，Assembly是啥。\
[Microsoft](https://learn.microsoft.com/en-us/dotnet/standard/assembly/)&[Microsoft2](https://learn.microsoft.com/en-us/dotnet/api/system.reflection.assembly?view=net-9.0)&[StackOverFlow](https://stackoverflow.com/questions/7954424/what-does-the-assembly-keyword-mean-in-the-assemblyinfo-cs-does-it-permit-to-us)

Assembly（程序集）：是 .NET 应用程序的打包和部署单元，是一个包含编译后的代码、元数据（程序集清单）和资源（如图片）的文件。它通常是 .exe（可执行文件）或 .dll（类库文件）。\
程序集清单（**Manifest**）： 描述程序集自身的信息，如身份标识（名称、版本）、包含哪些文件、依赖哪些其他程序集以及运行所需的安全权限等。\
类型元数据（**Type Metadata**）： 描述程序集中定义的所有类、方法、属性、字段等的信息。这是 .NET 反射机制的基础。\
中间语言代码（**IL Code**）： C# 代码被编译成与 CPU 无关的中间语言，而不是原生机器码。在运行时，CLR 的 JIT 编译器会将其编译为本地代码。\
资源（**Resources**）： 如图标、图片、本地化字符串等。

所以大概这个model已经是编译好的程序了，可能从其他地方来的，这个游戏只是拿出来运行它而已。


