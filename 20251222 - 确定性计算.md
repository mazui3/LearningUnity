确定性计算(deterministic algorithm)

在确定性计算中，只要**输入（Input）** 相同，且初始状态（Initial State）相同，那么计算出的 **结果（Output）** 就永远是完全一样的，而且中间经历的每一个步骤也完全一致。

准则：将“游戏逻辑（Logic）”与“表现层（View）”彻底分离。

(1)浮点数精度（Floating Point Issues）

精度差异： 不同的 CPU 架构（如 Intel 的 x86 和手机上的 ARM）、不同的操作系统，甚至是不同的编译器优化设置，对于同一行浮点数运算的结果可能会有极细微的差别（例如 $0.0000001$ 的差异）。\
连锁反应： 在游戏逻辑中，这个微小的差值会通过碰撞检测、伤害计算等逻辑迅速放大，导致几秒钟后两台客户端上的角色位置完全不同（即“掉线/不同步”）。\
解决方案： * 使用**定点数（Fixed-point math）**代替浮点数。使用专门的确定性数学库（如 DeterministicFloat）。\
例如，“角色以0.1米/秒的速度移动0.3秒”，可以转换成“角色以10厘米/毫秒的速度移动300毫秒”，从而避免浮点数的运算。

(2)随机数（RNG）

游戏经常会用到随机数，例如“技能‘斩龙诀’有30%的概率打出两倍伤害”，如果客户端各自随机，那么结果也会有所不同。\
种子管理： 绝对不能使用 Time.time 或系统时间作为随机种子。\
一种解决办法是，各客户端都使用同一套伪随机算法，具体做法是在战斗开始时，由服务端同步同一个随机种子，然后基于相同的规则生成随机数。

(3)遍历的顺序

乱序陷阱： 很多常用的数据结构（如 C# 的 Dictionary 或 C++ 的 std::unordered_map）在遍历时，其顺序是不确定的。\
后果： 假设你遍历一个列表给所有单位扣血，如果两台电脑遍历顺序不同，而其中一个单位的死亡触发了某个全局事件，那么两台电脑的事件触发顺序就会冲突。\
解决方案： 始终使用有序容器（如 List、Array），或在遍历前对键值进行排序。

如果使用诸如“for(int i=0;i<100;i++)”的语句遍历数组\
则可以确定数组的遍历顺序，但如果使用foreach语句遍历数组，则不能完全保证顺序，foreach只能保证把每个元素都遍历一遍。\
如果游戏逻辑依赖于遍历的顺序，foreach可能会导致不同的计算结果。

(4)固定步长的物理与逻辑（Fixed Timestep）\
远离变量 DeltaTime： 绝不能在逻辑计算中使用不稳定的帧间隔（如 Update(deltaTime)）。

解决方案： 必须使用固定的时间步长（Fixed Timestep）。无论硬件跑 30 帧还是 120 帧，逻辑更新的频率（比如每秒 60 次）必须恒定。

(5)多线程、异步和协程

由于多线程、异步和协程的调度并不由开发者控制，因此如果游戏逻辑中使用了这些技术，需要特别关注不同时间执行线程、异步和协程的代码是否会导致不同的运算结果。
