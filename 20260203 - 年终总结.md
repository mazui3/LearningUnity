问了deepseek大人常见bug类型。

## 常见的debug bug种类：

### 1. **逻辑错误类**
- 条件判断错误（`>`写成`>=`）
- 循环边界错误（off-by-one）
- 状态机状态错误

### 2. **内存/资源类**
- 内存泄漏
- 空指针/野指针
- 缓冲区溢出
- 资源未释放（文件句柄、数据库连接）

### 3. **并发/多线程类**
- 竞态条件
- 死锁
- 线程安全问题

### 4. **接口/数据流类**（你这个就是这类！）
- 参数未使用（像你遇到的）
- 返回值错误/缺失
- 类型不匹配
- API调用顺序错误

### 5. **初始化/状态类**
- 变量未初始化
- 状态不一致
- 默认值错误

### 6. **性能类**
- 无限循环
- 算法复杂度高
- 重复计算

### 7. **环境/配置类**
- 路径/配置错误
- 依赖版本问题
- 环境变量缺失

好像也不是一次被参数未使用或者未连上这样的bug背刺了。

得相信断点，断到的，才是真实的。
