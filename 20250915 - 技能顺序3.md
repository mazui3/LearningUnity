今天才看懂技能分为了三种：
 - 属性变化
 - 获得功能
 - 触发行为\
这三种。

策划习惯性按顺序设计了一组技能：
1. （触发某些条件后）加buff
2. 按照buff判定功能1 - 触发行为
3. 按照buff判定功能2 - 属性变化
4. 移除buff

以上都在同一个触发时机内。\
触发行为可以被触发，属性变化就是没有。\
发现属性变化是在使用属性时才会计算。\
比如要用到攻击力了，在Formula Chain里面寻找关于加攻击力的技能，全部触发一遍，计算攻击力。\
所以策划写的技能会变成。1，2，4，（开始计算攻击相关数值了）3，这样触发，导致3因为没有buff了而没被读取。

感觉它三种技能该分的更好一些。
