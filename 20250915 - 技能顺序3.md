今天才看懂技能分为了三种：
 - 属性变化
 - 获得功能
 - 触发行为\
这三种。

策划习惯性按顺序设计了一组技能：
1. （触发某些条件后）加buff
2. 按照buff判定功能1 - 触发行为
3. 按照buff判定功能2 - 属性变化
4. 移除buff

以上都在同一个触发时机内。\
触发行为可以被触发，属性变化就是没有。\
发现属性变化是在使用属性时才会计算。\
比如要用到攻击力了，在Formula Chain里面寻找关于加攻击力的技能，全部触发一遍，计算攻击力。\
所以策划写的技能会变成。1，2，4，（开始计算攻击相关数值了）3，这样触发，导致3因为没有buff了而没被读取。

感觉它三种技能该分的更好一些。

\
后补：\
在技能判定method里，一直有两个时机variable。\
一个叫mCurAtkEvent，一个叫mCurPiecesEvent。\
当前在计算攻击流程时的时机，和当前棋子的时机，这两个。\
（记得很久之前修过个bug，是因为mCurAtkEvent跟mCurPiecesEvent一个更改了时机而一个没有，判定一直为true，导致连续触发）\
但问题是，在处理最后结算用的属性数值技能时，mCurAtkEvent已经被清空了，导致属性类的技能没法触发，改成了mCurPiecesEvent。\
师父之前觉得只保留mCurPiecesEvent，不需要mCurAtkEvent。也有个bug得用mCurAtkEvent才行。\
有机会挖一挖……
