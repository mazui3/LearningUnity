1. **Pipeline**\
   Pipe是一种把数据整合起来的写法，特点是用 | 隔开各个参数。\
   _是否必须得是 | ?_ \
   不一定，但 | 是最常见的，也是咱代码里约定的。\
   _Pipe的参数是否有顺序?_ \
   不一定，但咱代码里约定的，分割技能参数用的pipe没有顺序。

2. **写参数时的格式**\
   我们写参数时等号前后不能有空格。\
   _必须得没有空格吗？_\
   不一定，我们代码里没有做Trim(), Replace()这种常见的防字符外输入的保护，所以得麻烦各位注意格式。\
   如果使用特殊符号，例如逗号，方括号，得使用英文下的特殊符号。\
   应该咱不会出现但有可能会出现肉眼看起来一摸一样的符号。\
   但读取Unicode时不一样的情况。实在想不出来为什么输入会报错时，可以查看输入字符的Unicode。

2.5 **参数**
   参数为什么不写空值保护/格式保护。\
   有时不通过错误的输入也是一种自我查证。直接报空比起游戏运行得不对更容易找到问题。

3. **字典**\
   字典是一种数据结构，每一组数据由一个key和一个值组成。\
   字典里的key不能重复。\
   技能id，buff id，将领id这类用字典装的数据，id不能重复。

4. **技能执行顺序**\
   一般来说，技能按照frame（excel里面的行数）从上到下执行。\
   如果有技能在frame 1动了frame 2的条件，frame 2会有影响。\
   但只有数值类技能，是在计算时被结算的。例如“加20攻击力”，只会在使用攻击力时被结算。

5. **生效条件和生效条件值**\
   有些种类生效条件是只判定0和1 - 是否满足，有些种类是判定满足层数。各个种类处理方式不同。\
   效果是否会处理满足层数，各个效果处理方式不同，没生效时需要具体分析。

6. **1和201**\
   凯撒中的时机有0+和200+两种应该是一样功能，但新和旧的两种时机。\
   代码中的运算区别在于，判定当前单位事件和当前攻击事件。\
   表里推荐用200+而不是0+，避免效果多次触发。\
   我觉得是0+作用于数值类技能，200+作用于功能性技能。
