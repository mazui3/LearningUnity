1. Pipeline
   Pipe是一种把数据整合起来的写法，特点是用 | 隔开各个参数。\
   是否必须得是 | ? :不一定，但|是最常见的，也是咱代码里约定的。\
   Pipe的参数是否有顺序? ：不一定，但咱代码里约定的，分割技能参数用的pipe没有顺序。\
   \
3. 写参数时的格式
   我们写参数时等号前后不能有空格。\
   必须得没有空格吗？：不一定，我们代码里没有做Trim(), Replace()这种常见的防字符外输入的保护，所以得麻烦各位注意格式。\
   如果使用特殊符号，例如逗号，方括号，得使用英文下的特殊符号。\
   应该不会出现但有可能会出现肉眼看起来一摸一样的符号，但读取Unicode时不一样的情况。实在想不出来为什么输入会报错时，可以查看输入字符的Unicode。
