deepseek太牛逼了，市场还需要我吗。\

```
if (Input.GetMouseButton (0))
```
检查鼠标左键是否被按住（按下状态）.\
参数解释,0：表示鼠标左键
- 0 = 左键
- 1 = 右键
- 2 = 中键\

- 与其他鼠标函数的区别
| 函数              | 触发时机 | 使用场景 |
| :---------------- | :------: | ----: |
| Input.GetMouseButton(0)        |  按住期间每帧都返回true   | 拖拽、持续旋转、长按 |
| Input.GetMouseButtonDown(0)           |   按下瞬间只触发一帧   | 点击、选择、开始拖拽 |
| Input.GetMouseButtonUp(0)    |  松开瞬间只触发一帧   | 结束拖拽、释放 |

```
void Update()
{
    if (Input.GetMouseButton(0)) // 如果鼠标左键被按住
    {
        // 这里写拖拽逻辑
        Debug.Log("正在拖拽...");
        
        // 通常配合鼠标位置使用
        Vector2 mousePos = Input.mousePosition;
        // 执行拖拽计算...
    }
    
    if (Input.GetMouseButtonDown(0)) // 鼠标左键刚按下
    {
        Debug.Log("开始拖拽");
    }
    
    if (Input.GetMouseButtonUp(0)) // 鼠标左键刚松开
    {
        Debug.Log("结束拖拽");
    }
}
```

注意拥有了mousePos的时候，在运算拖拽之前，要看一下是否为零。
```
Vector2 mousepos = Vector2.zero;

if (mousepos.magnitude == 0)//检查 mousepos向量是否为零向量（初始状态）
{
  mResetOffSet = false;
  mousepos = Input.mousePosition;// 初始化鼠标位置
}
else
{
  //真的移动移动
}
```

Magnitude的用法：
```
// 这两种写法等价
if (mousepos.magnitude == 0)
// 等同于
if (mousepos.x == 0 && mousepos.y == 0 && mousepos.z == 0)
```
为什么要这样检查？避免首次点击时的突然跳动：如果不初始化，第一帧的offset会从(0,0,0)跳到当前鼠标位置，产生很大的跳跃

由于浮点数精度问题，更安全的写法是
```
if (mousepos.magnitude < 0.001f) // 使用小的容差值
{
    // 认为是零向量
}
```

然后才：
```
if (Input.GetMouseButton(0)) // 当按住鼠标时
{
    Vector2 p = Input.mousePosition;
    Vector3 offset = p - mousepos;
    // ...执行拖拽计算
}
```

**获取鼠标位移**
```
Vector2 p = Input.mousePosition;
Vector3 offset = p - mousepos;
mousepos = p;
```
计算与上一帧位置的偏移量,更新鼠标位置记录,用这个偏移量来告诉摄影机要怎么移动。

**相机参数计算**
```
float _halfFOVTan = Mathf.Tan((m_camera.fieldOfView * 0.5f) * Mathf.Deg2Rad);
float hight = m_camera.transform.position.y * _halfFOVTan * 2.0f;
float width = hight * m_camera.aspect;
```
计算半视角正切值：将FOV(Field of View)角度转换为弧度并计算正切\
计算视口高度：基于相机高度和FOV计算相机视锥体在相机高度平面上的高度\
计算视口宽度：通过宽高比计算宽度\
>https://zhuanlan.zhihu.com/p/679453452

**屏幕偏移到世界空间的转换**
```
offset.x = -offset.x * 2/ Screen.width * width;
offset.z = -offset.y / Screen.height * hight * Mathf.Tan(m_camera.fieldOfView * Mathf.Deg2Rad);
offset.y = 0;
```
X轴转换：将屏幕像素偏移转换为世界空间X轴偏移
- offset.x：反转方向（通常屏幕坐标与3D坐标方向相反）
- 2 / Screen.width：归一化到[-1, 1]范围
- * width：乘以世界空间宽度得到实际偏移量\

Z轴转换：类似X轴但使用Y屏幕偏移
- 额外乘以Mathf.Tan(m_camera.fieldOfView * Mathf.Deg2Rad)来校正透视效果

```
m_dir = offset;
m_smoothSpd = offset.magnitude / Time.deltaTime;
Vector3 psrc = transform.position;
transform.Translate(new Vector3(offset.x * touchScaleHorizontal, 0, offset.z * touchScaleVertical), Space.World);
```

透视校正：考虑了相机的FOV和位置，确保拖拽在不同距离下保持一致的视觉感受\
屏幕比例适配：使用Screen.width/height确保在不同分辨率下行为一致\
可调节参数：通过touchScaleHorizontal/touchScaleVertical控制灵敏度\
平滑处理：记录速度和方向用于可能的平滑过渡\
