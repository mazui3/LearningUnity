- IsSkillTriggerConditionAchieved
- IsSkillEffectTriggerCmpt

两大判定头头。\
因为method名读不懂，一直搞混，直到deepseek说
>"Cmpt" 很可能是 "Component"（组件） 的缩写。

你说的有道理。

判断是否达成触发条件
```cs
//咦这个跟skillLevel还有关系？
public int IsSkillTriggerConditionAchieved(SkillFramConfig sfc, PiecesLogic userPl, PiecesLogic target,
        int skillLevel, bool isGetValue = false)
{
        int conditionCount = sfc.SkillConditionList.Count;
        for (int conditionIndex = 0; conditionIndex < conditionCount; conditionIndex++)
        {
            switch (effectCondition)
            {
                case SkillTrigger.None:
                  isConditionAchieved = 1;
                  break;  
            }                
        }
}
```
这里的condition包括了
- 在特定地形中时生效
- 士气与特定值进行比较，满足时生效
- 比较游戏内资源\
这类

condition可为复数。\

效果触发时机
```cs
public bool IsSkillEffectTriggerCmpt(SkillFramConfig sfc, PiecesLogic pluser, effectTrigger successTrigger, PiecesLogic target, bool isFromGetSkillVal = false)
{

}
```
