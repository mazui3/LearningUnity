今天写了个类似的内容，玩家在商店购买东西，数据会连接playfab。\
在这个数据传输中如果掉线，那可能会导致货币扣除但奖励未发放等情况。

看到数据上传是一个uniTask（用了CBS插件）
```cs
public UniTask<CBSBaseResult> Upload<T>(T t, bool withLoading = true) where T : class, ICustomData
{
  var request = CBSRequestManager.Instance.GetRequest<SaveProfileDataRequest>();
  request.Key = CustomDataManager.GetKey(t);
  request.Value = CustomDataManager.ToJson(t);
  if (withLoading)
  {
    return CBSRequestManager.Instance.CallUntilSuccessfulWithLoadingAsync<CBSBaseResult>(request, -1);
  }
  else
  {
      return CBSRequestManager.Instance.CallAsync<CBSBaseResult>(request);
  }
}
```

因为return的是个uniTask所以可以这么做。
```cs
//最好是之前把数据都打包好
PlayerDataManager.Instance.Update(data).GetAwaiter().OnComplete( () =>
{
  //如果玩家的数据都ok的话，在游戏内发放奖励
  Global.AddReward(someReward);
});
```

这是比较理想的状态……但一看班码给后台的数据可能会发两份，那很难搞了。
