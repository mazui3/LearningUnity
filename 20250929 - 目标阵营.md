targetCamp
>技能效果可以影响的单位所属阵营：
>0为使用者自身，1为使用者的友军，2为使用者的敌人，3为以地面为目标，4为当前与自身战斗的敌方单位

觉得这个设计很合理哇。\
但一看哇怎么感觉跟属性类技能又双叒叕不兼容。\

我们来复习一遍。

有一个叫GetSkillTarget的method用于判定target是谁，很有道理。\

第一条路，**从onPiecesEvent开始**
```cs
case PEvent.PEvent_piecesDie:
{
  //这些是放在skillManager之下的var
  if (target == mCurStrategyPieces)
  {
      mCurStrategyPieces = null;
      mCurUsedStrategySkill = null;
  }

  //从DataBundle中读出来的pl
  target = data.getValue<PiecesLogic>("die");
  attacker = data.getValue<PiecesLogic>("killer");

  List<PiecesLogic> tempPls = getSkillUserWitTargetTrigger(effectTrigger.effectTrigger_selfDie);
  if (tempPls != null)
  {
    for (int i = 0; i < tempPls.Count; i++)
    {
      if (tempPls[i] == target)
      {
          handlePiecesSkillWithTargetEffectTrigger(tempPls[i], effectTrigger.effectTrigger_selfDie,null);
      }
    }
  }

  checkRemoveRangeSkillPieces(target);
  checkRemoveRecodeEffectTriggerPieces(target);

  //所以handlePiecesSkill跟handlePiecesSkillWithTargetEffectTrigger是类似但分开的…
  handlePiecesSkill(attacker, target);

  //这些都是按时机来处理的effectTrigger我想
  pls = getSkillUserWitTargetTrigger(effectTrigger.effectTrigger_surroundFriendDied);
  if (pls != null)
  {
    for (int i = 0; i < pls.Count; i++)
    {
        handlePiecesSkillWithTargetEffectTrigger(pls[i], effectTrigger.effectTrigger_surroundFriendDied, target);
    }    
  }

  pls = getSkillUserWitTargetTrigger(effectTrigger.effectTrigger_surroundPieceDied);
  if (pls != null)
  {
    for (int i = 0; i < pls.Count; i++)
    {
      if (pls[i] != attacker)
      {
          handlePiecesSkillWithTargetEffectTrigger(pls[i], effectTrigger.effectTrigger_surroundPieceDied, target);
      }
    }    
  }

  if (mCurUsedMianSkill.ContainsKey(target))
  {
      mCurUsedMianSkill.Remove(target);
  }
}
```

于是我们有handlePiecesSkill和handlePiecesSkillWithTargetEffectTrigger进一步处理。\
先：寻找技能\
后：触发技能

handlePiecesSkill。\
注释上写的是：处理某个单位身上的技能-只能处理作用在自己身上的效果，如果作用在敌方身上不能调用此函数\
啊这样的吗。
