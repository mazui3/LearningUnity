adjust sdk,打点用。
>https://dev.adjust.com/en/sdk/unity

神他妈忘记init了。\
班码会在mainCamera中init，怎么忘记init Adjust。\
Adjust是用prefab自带script运行的，在要跑的主界面里生成prefab就成。

```cs
if(GameObject.Find("Adjust") != null)
  return;
GameObject adjustPrefab = Resources.Load<GameObject>("Adjust");

adjustPrefab = Instantiate(adjustPrefab);
adjustPrefab.name = "Adjust";
```

\
\
今天把adjust修好了，还是得看log啊！！！(debug守则第一条）\
不过有个问题，adjust里面的documentation有android SDK, Unity SDK两项。\
按常理读了Unity SDK这页，把东西装了发现漏了class。\
deepseek说缺了dependency感觉不对哇，翻到adjust unity package里面有个dependency的xml里写了的确有个没装过的class的名字。\
在Android SDK中翻到了装它的说明。\
なぜ？\
还有个错误，因为参考了旧项目里的adjust装法。\
旧项目的adjust版本跟最新版本不同了，旧项目内不需要添加adjust class dependency，所以不确定。\
**不同版本是不一样的参考时不要想当然。**

\
\
哈哈，犯蠢了，自己测完log后忘记从sandbox模式调到production了。

班码里adjust的使用是先打上gobal listener，再ask adjust permission，同意了后再生成adjust（why？）
```cs
//先挂上listener
BroadcastCenter.addListener(ConstantData.GETADJUSTPLAYERPERMISSION, InitAdjustSDK);
//然后我们去问permission
InitPermissionHelper();
```

```cs
private void InitPermissionHelper()
{
  if(GameObject.Find("PermissionHelper") != null)
      return;
  GameObject obj = new GameObject("PermissionHelper");
  obj.name = "PermissionHelper";
  GameObject.DontDestroyOnLoad(obj);
  obj.AddComponent<RequestPermissionScript>();
}
```

RequestPermissionScript是个MonoBehaviour，是自己写的处理permission用的。
