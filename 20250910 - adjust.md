adjust sdk,打点用。
>https://dev.adjust.com/en/sdk/unity

神他妈忘记init了。\
班码会在mainCamera中init，怎么忘记init Adjust。\
Adjust是用prefab自带script运行的，在要跑的主界面里生成prefab就成。\

```cs
if(GameObject.Find("Adjust") != null)
  return;
GameObject adjustPrefab = Resources.Load<GameObject>("Adjust");

adjustPrefab = Instantiate(adjustPrefab);
adjustPrefab.name = "Adjust";
```
