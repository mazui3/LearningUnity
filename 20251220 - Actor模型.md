Actor模型的理念 —— 万物皆Actor，它是更进一步的面向对象， 即把世间万物都当作Actor对象。

演员是一个运算实体，响应接收到的消息，相互间是并发的：

每个 Actor 都有三个核心能力：

1. **发送消息**：给其他的 Actor 发邮件。
2. **接收消息**：处理别人发来的邮件。
3. **创建更多 Actor**：像细胞分裂一样，招募新的小人来帮忙。

发送者与已发送通信的解耦，是演员模型的根本优势，演员模型启用了异步通信并将控制结构当作消息传递的模式[2]。

消息接收者是通过地址区分的，有时也被称作“邮件地址”。因此演员只能和它拥有地址的演员通信。\
它可以通过接收到的信息获取地址，或者获取它创建的演员的地址。

---

Actor 模型之所以强大，是因为它严格遵守以下规则：

* **隔离性（私有状态）**：每个 Actor 的数据都是私有的。你不能直接修改别人的变量，只能通过发消息请求对方修改。这彻底解决了“资源竞争”的问题。
* **异步性**：我给你发信后，不需要等回信，我可以继续干我的活。等你有空处理了，再给我回一封信。
* **顺序性**：虽然一个 Actor 可以收到很多信，但它一次只读一封信，按顺序处理。这样 Actor 内部就不需要“加锁”了。

| 特性 | 传统多线程模式 | Actor 模型 |
| --- | --- | --- |
| **共享资源** | 共享内存（像共享笔记本） | 不共享（每个人只有自己的本子） |
| **同步方式** | 使用 **Lock (锁)**，容易死锁 | **异步消息**，无锁设计 |
| **扩展性** | 很难扩展到多台机器 | 天然支持分布式（发给本地或远程 Actor 没区别） |

