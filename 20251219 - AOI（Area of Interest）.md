**AOI 算法的核心目的就是：只把“你关心的”信息发给你。**

---

## 常见的 AOI 实现方案

目前主流的 AOI 算法主要有以下几种：

### 1. 九宫格法 (Grid-based)

这是最常用、实现最简单的方案。

* **原理：** 将整个地图划分为大小相等的方格。
* **逻辑：** 玩家位于某个格子时，他能看到的信息包括**当前格子**以及**周围相邻的 8 个格子**（共 9 个格子）。
* **优点：** 计算极其简单，性能极高。
* **缺点：** 如果格子划分太粗，不够精准；如果太细，跨越格子时的处理会变多。

### 2. 十字链表法 (Cross-linked List)

这种方法在很多老牌 MMORPG 中非常经典。

* **原理：** 将所有对象的 X 坐标和 Y 坐标分别存入两个有序链表。
* **逻辑：** 当一个对象移动时，在两个链表上移动节点位置。通过查找 X 轴和 Y 轴上在玩家视野半径（Radius）内的节点，取交集得出结果。
* **优点：** 坐标精确，不依赖地图大小。
* **缺点：** 每次移动都需要重新排序链表，当单位密度极高时，性能消耗会增加。

### 3. 灯塔/塔式法 (Tower/Quadtree)

* **原理：** 类似于四叉树（Quadtree）。将大地图不断细分，直到每个小区域内的单位数量在可控范围内。
* **逻辑：** 当你移动时，服务器会去查询你所在节点及其父节点的相邻节点。
* **优点：** 适合地图巨大且单位分布不均匀的场景。

---

## 方案对比

| 特性 | 九宫格 (Grid) | 十字链表 (Linked List) | 四叉树 (Quadtree) |
| --- | --- | --- | --- |
| **实现难度** | 简单 | 中等 | 较复杂 |
| **内存占用** | 较低 | 中等 | 较高 |
| **适用场景** | 大多数竞技游戏、常规网游 | 精确度要求高的场景 | 地图巨大、单位稀疏的场景 |
| **性能瓶颈** | 跨越格子时的瞬间压力 | 高密度单位下的排序耗时 | 树节点的频繁维护 |

