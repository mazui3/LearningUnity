```
private void onPiecesEvent(PEvent e, DataBundle data)
{
  switch (e)
  {
    case PEvent.PEvent_piecesDie:

    target = data.getValue<PiecesLogic>("die");
    attacker = data.getValue<PiecesLogic>("killer");

    //这里，我们会处理攻击者身上的技能
    handlePiecesSkill(attacker, target);
    //然后，处理被攻击者身上的技能，很make sense
    handlePiecesSkill(target, attacker);
    //不过昨天误以为以上两行代码中，target的技能多了一个（可能也有），buff加上了技能，技能被触发。再触发攻击者身上技能的时候二度触发，可不可能有这个问题。

    //处理周边影响单位
    pls = getSkillUserWitTargetTrigger(effectTrigger.effectTrigger_SurroundPieceDead);
    //loop一遍再handlePiecesSkill(pls[i], target, target);

    //还有个主动技能的全局字典处理，俺不大喜欢说实话
    if (mCurUsedMianSkill.ContainsKey(target))
    {
        mCurUsedMianSkill.Remove(target);
    }
  }
}
```
问题是有一个主动技能A，在攻击者身上，给选择一个单位，给被攻击者加一个buff。
>选择3格范围内的一个敌方单位，令其受到的伤害增加{0}并混乱2回合
>若2回合内该单位被消灭，则对该单位周围2格内所有敌方单位造成{1}点的固定伤害

这个buff会给被选中者添加技能B。\
加buff这个event中，加上buff后，会触发一次这个buff（其实也make sense）。\
在处理死亡时，加buff的技能A还在攻击者身上（且没有分触发时机）。\
所以变成了。\
死亡，处理攻击者技能->触发技能A ->处理buff加上B-> 满足技能B的条件，触发B，扣血。\
处理被攻击者的技能B->扣血。\
扣了两遍。

这个合成大bug里有几个方向可以考虑的。\
首先buff系统加技能，这样加不好。\
现在的buff系统中只有1.改变属性（比如攻击力，防御力，移动力这样的属性）2.添加功能（无视伏击，不能被斩杀），和加技能。\
没有3.触发行为大类的。\
为什么没有？是否应该有？如果有，跟技能的结算时机？方式？

第二个是，主动技能的处理时机，主动技能如果可以在比如战斗开始前结算，那不会在死亡时再被触发。\

第三个方向，主动技能为什么在死亡时可以被触发？\
这个主动技能的需求是，选择一个单位加上buff。在结算死亡时，攻击这个攻击单位，和选择加buff单位，是否是混在一起了？

嗯，感觉第三个方向更像是bug。
