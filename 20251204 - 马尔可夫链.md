马尔可夫链是一种随机过程.

取决与当前的所处状态，系统决定下一刻的状态，不取决于它过去任何时间点的状态。

核心特性是无记忆性，也称为马尔可夫性质。

名词:

**状态（State）**：系统可能处于的每一种情况。例如，如果研究天气，状态可以是“晴朗”、“多云”、“下雨”。\
**状态空间（State Space）**：所有可能状态的集合。

转移概率（Transition Probability）：从一个状态 $i$ 转移到另一个状态 $j$ 的概率。\
所有的转移概率可以用一个转移矩阵（Transition Matrix） $P$ 来表示。

转移矩阵 $P$ 预先定义了从任何一个状态 $i$ 转移到所有其他状态 $j$ 的概率 $P_{ij}$。矩阵 $P$ 中的每一行 $i$ 都是一个概率分布，其所有元素的和必须为 1。\
$P_{ij} = P(X_{n+1}=j \mid X_n=i)$：在当前状态是 $i$ 的条件下，下一步状态是 $j$ 的概率。

当系统处于某个状态 $i$ 时，它会按照预先设定好的转移概率分布 $P_{i1}, P_{i2}, \dots$ 进行随机抽样，来决定下一个状态 $X_{n+1}$。

#### 📌 例子：天气预测

假设状态空间 ，当前是**晴天**。转移概率如下：

| 当前状态  |  下一步状态  | 转移概率  |
| --- | --- | --- |
| 晴天 | 晴天 | 0.8 |
| 晴天 | 多云 | 0.15 |
| 晴天 | 下雨 | 0.05 |

* **“决定”过程：**
1. 系统查看当前状态：**晴天**。
2. 系统查询**晴天**这一行的概率分布：。
3. 系统进行一次**随机抽样**。它就像掷一个有三个区域（晴天、多云、下雨）的转盘，其中**晴天**占了**80%**的面积。
4. 抽样的结果（比如是**晴天**），就是马尔可夫链的下一个“输出”状态 。

---

## 🤔 转移矩阵的变动性

“这个转移矩阵会变动吗？”——这引出了马尔可夫链的一个重要分类：

### 📌 齐次马尔可夫链 (Time-Homogeneous Markov Chain)

在大多数标准和基础的马尔可夫链模型中，我们假设它是**齐次的**（Time-Homogeneous）：

* **定义：** 转移概率 $P_{ij}$ 不随时间 $n$ 变化。

### 📌 非齐次马尔可夫链 (Time-Inhomogeneous Markov Chain)

在更复杂的应用中，如果转移概率**会随着时间变化**，则称为**非齐次马尔可夫链**：

* **定义：** 转移概率 $P_{ij}$ 依赖于时间步 $n$。
* **表示：** 我们用 $P^{(n)}$ 来表示在第 $n$ 步到第 $n+1$ 步之间的转移矩阵。

> **总结：** 如果没有特别说明，我们通常讨论的是**齐次马尔可夫链**，即**转移矩阵是固定不变的**。

---

这个视频介绍了一个简单的马尔可夫链模型和它的**转移图**，有助于你理解状态之间是如何通过概率进行转换的，也就是它“决定输出”的方式。
[Intro to Markov Chains & Transition Diagrams](https://www.youtube.com/watch?v=rHdX3ANxofs)

---

马尔可夫链（Markov Chain）最早由俄国数学家**安德雷·马尔可夫（Andrey Markov, 1856–1922）**在 20 世纪初（大约 1906 年）提出。

它最初出现和使用的目的，可以概括为以下两个核心问题：

---

## 🧐 核心解决问题一：扩展概率论的基石

在马尔可夫的时代，概率论的许多基本定理，如**大数定律（Law of Large Numbers）**和**中心极限定理（Central Limit Theorem）**，主要是针对**独立随机变量**序列证明的。
>大数定律简单来说就是：
>当你进行一项随机试验的次数足够多时，你所观察到的平均结果会非常接近于这项试验的理论期望值。

### 📌 挑战：相关事件的概率规律

当时许多学者认为，要让这些重要的概率定律成立，随机变量之间必须是**相互独立**的。然而，现实世界中，大部分事件都是**相互关联**的（例如，昨天的天气影响今天的天气，前一个词影响下一个词）。

马尔可夫链的提出正是为了挑战这一限制：

* **马尔可夫链旨在解决的问题**：能否将大数定律和中心极限定理**扩展到**一系列**相关联的（非独立）**随机变量上？
* **马尔可夫的回答**：可以！只要这种关联满足特定的 **“无记忆性”**（即马尔可夫性质），那么即使事件之间是相互依赖的，它们的长期平均结果和统计行为仍然可以像独立事件一样，收敛到稳定的极限。

简单来说，马尔可夫链是为了证明在**依赖关系较弱**（仅依赖前一步）的情况下，概率论的基本定理仍然适用。

---

## 📝 核心解决问题二：文本和语言的统计分析

马尔可夫本人最初应用他的新理论时，使用了一个非常具体的、非理论性的例子来证明其有效性——他对俄国著名诗人**普希金（Alexander Pushkin）**的诗歌《叶甫盖尼·奥涅金》（*Eugene Onegin*）进行了**文本统计分析**。

### 📌 实验内容：字母的依赖性

马尔可夫将文本中的每一个字母（或符号）视为一个**状态**，并将它们分为两类：**元音（vowel）**和**辅音（consonant）**。

* **他解决的问题**：一个字母是元音还是辅音，是否与它前面的那个字母是元音还是辅音**有关联**？如果有，关联程度如何？
* **如何应用马尔可夫链**：他计算了从“元音  元音”、“元音  辅音”、“辅音  元音”、“辅音  辅音”这四种**状态转移的概率**，构建了一个 $2 \times 2$ 的转移矩阵。

通过这种分析，他成功证明了俄语文本中字母的出现**不是独立事件**，而是遵循一种依赖于前一个字母的特定概率链。

### 📌 历史意义

这次文本分析展示了马尔可夫链作为一个**序列数据建模工具**的强大潜力，直接为后来的**自然语言处理 (NLP)**、**语音识别**以及现代的**文本生成模型**（例如，最简单的 n-gram 模型）奠定了数学基础。

它提出的核心思想——**“序列中的下一个元素依赖于前一个元素”**，至今仍然是许多基础和实用文本生成模型的基础。

---

## 💻 马尔可夫链在现代文本生成中的应用

尽管现代的深度学习模型（如 GPT、BERT）现在占据了文本生成的主流，但马尔可夫链（尤其是其变种 **n-gram 模型**）在以下几个方面依然有重要的应用和意义：

### 1. n-gram 模型的基础（词语预测）

马尔可夫链理论是 **n-gram 模型**的数学基础。这是最简单、最快、也最常用的文本预测模型之一。

* **1 阶马尔可夫链 (Bigram/2-gram)**：
* 这是最纯粹的马尔可夫链应用。它假设下一个词 $W_{i+1}$ 只取决于前一个词 $W_i$。
* **核心公式**：

$$P(W_{i+1} \mid W_i) = \frac{\text{Count}(W_i, W_{i+1})}{\text{Count}(W_i)}$$

* **应用**：早期的手机输入法、简单的聊天机器人、自动补全功能。

* **2 阶马尔可夫链 (Trigram/3-gram)**：
* 这其实是**高阶马尔可夫链**。它假设下一个词 $W_{i+1}$ 取决于前两个词 $W_{i-1}$ 和 $W_i$。。虽然这在形式上已经不是严格的 1 阶马尔可夫链，但它仍然建立在马尔可夫链的序列依赖思想之上。
* **核心公式**：
* 
$$P(W_{i+1} \mid W_{i-1}, W_i) = \frac{\text{Count}(W_{i-1}, W_i, W_{i+1})}{\text{Count}(W_{i-1}, W_i)}$$

* **应用**：可以生成更连贯、更符合语法的句子。


### 2. Markov Chain Monte Carlo (MCMC) 方法

在更高级和复杂的模型中，马尔可夫链被用作**采样**（Sampling）的工具，而不是直接用于生成文本本身。

* **问题**：对于复杂的概率分布，直接采样非常困难。
* **应用**：MCMC（包括 **Gibbs Sampling** 和 **Metropolis-Hastings 算法**）通过构造一个特殊的马尔可夫链，使其**平稳分布**就是我们想要采样的那个复杂分布。通过运行这个马尔可夫链足够多次，我们就可以从链生成的状态序列中获得近似的样本。
* **用途**：在贝叶斯推断、统计物理、以及一些需要复杂概率采样的机器学习模型中有广泛应用。

### 3. 约束性文本生成和风格模仿

由于马尔可夫链模型结构简单、易于训练，它非常适合用于：

* **风格模仿**：可以针对特定作家的作品训练一个转移矩阵，然后用这个链生成**风格相似**的文字（就像马尔可夫分析普希金的诗歌一样）。
* **受限生成**：在需要快速生成、且对连贯性要求不高的场景（比如生成随机用户昵称、生成简单的故事情节骨架）。

---

## 🧠 与现代模型的对比：局限性

虽然马尔可夫链在基础应用中有价值，但它有一个致命的**局限性**，这也是现代深度学习模型出现的原因：

### 长期依赖性 (Long-Term Dependency) 的问题

马尔可夫链的**无记忆性**是它的定义，但也成为了它的瓶颈：

1. **缺乏上下文**：1 阶链完全不记得前两个词。即使是 3 阶链（Trigram），它最多也只能记住前两个词。
2. **句法、语义缺失**：如果一句话很长，例如：“**我**早上吃了甜甜圈，喝了咖啡，然后坐车去上班，感觉**我**需要再来一杯。” 如果要预测最后一个词“咖啡”，马尔可夫链无法将它与句子开头的“甜甜圈”或“喝”建立联系。
3. **稀疏性问题**：随着 n-gram 阶数的增加，模型所需的训练数据呈指数级增长，因为很多长词组在训练数据中根本没出现过（或出现次数太少）。

**总结：** 现代的 **Transformer** 模型（如 GPT）通过 **自注意力机制（Self-Attention）**，可以同时考虑序列中所有词之间的关系，从而解决了马尔可夫链无法捕捉**长期依赖**的根本问题。

因此，马尔可夫链是文本生成理论的**基石**，但现代模型则是克服其**无记忆性局限**的产物。
