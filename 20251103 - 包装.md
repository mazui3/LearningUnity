今天改了个大的。

琢磨了想……如果要储存一个单位类似的数据，比起
```
private Dictionary<int, int> skillCoolDown = new Dictionary<int, int>();
private Dictionary<int, int> skillRoundNum = new Dictionary<int, int>();
```
不如把CoolDown和RoundNum wrap成一个class。\
之后给class加东西也方便。

然后是储存，老项目储存是按二进制位置储存的，一个错位就load错东西，也不敢加新field。
\
新项目用的储存是啥来着，有空几个笔记，受够这个二进制了…
