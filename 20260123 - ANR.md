ANR 是 Application Not Responding（应用程序无响应）的缩写，是 Android 系统特有的一种性能异常现象。

当应用程序的主线程（UI线程）被长时间阻塞（如执行了耗时操作、死锁或CPU过高），导致无法及时响应用户输入事件（触摸、按键）或系统事件时，系统会弹出ANR对话框，提示用户“强制停止”或“等待”。 

同事哥说安卓方会按照百分五（比例）如果遇到卡顿，会报ANR问题。

会给线程log，但线程log跟代码中的函数完全对不上号，只看log很难修。

说打包的时候把create symbols.zip从disabled改成public，安卓方就会反馈游戏里的函数报错了。
>不过同事哥说最近改了也没用
>用了一个unity cloud方平台的sdk，平台会把报错数据统计出来，包括使用的设备，报错的函数，非常方便，有空查一查。

在班中ANR问题大多因为动画等待时间和第三方sdk导致的。

动画等待如果在主线程里等，的确会直白的卡住。

然后用aysnc去叫打开新窗口。

结果发现aysnc会在下一帧触发，而在下一帧之前需要的数据被清空了。

记得需要读取的数据做数据copy而不是单纯指针……
>c#里指针太好用了，没怎么做copy了。
