Q:元数据是什么
>A:元数据就是"关于数据的数据" 或者 "描述信息的信息"。

一本书：书的内容是数据\
书的版权页（作者、出版社、ISBN、出版日期）就是元数据

一张照片：像素颜色是数据\
照片的EXIF信息（拍摄时间、相机型号、GPS位置）就是元数据

在C#/.NET中，元数据是描述代码本身的信息，而不是代码要执行的逻辑。

```cs
public class Person
{
    // 这些都是"数据"或"逻辑"
    public string Name { get; set; }
    public int Age { get; set; }
    
    public void SayHello()
    {
        Console.WriteLine($"Hello, I'm {Name}");
    }
}
```

```cs
// 这些都是"元数据" - 描述Person类的额外信息
[Serializable]           // 描述：这个类可以被序列化
[Obsolete("Use Human class instead")] // 描述：这个类已过时
[DisplayName("人员信息")] // 描述：在UI中显示为"人员信息"
public class Person
{
    // ...
}
```
Serializable倒是已经用的很多了。

Deepseek还讲得挺多的！它说分编译器自动拥有的元数据和自己加的元数据。
```cs
public class Calculator
{
    // 编译器为这个类自动生成的元数据包括：
    // - 类名：Calculator
    // - 命名空间
    // - 方法名：Add
    // - 参数类型：(int, int)
    // - 返回类型：int
    // - 访问修饰符：public
    public int Add(int a, int b) => a + b;
}
```

```cs
[Author("Alice", Version = 2.0)]    // 元数据：作者是Alice，版本2.0
[Category("Math Utilities")]        // 元数据：属于数学工具类别
public class AdvancedCalculator
{
    [Range(0, 100)]                 // 元数据：参数应该在0-100之间
    public int Calculate(int value) => value * 2;
}
```

元数据可以：\
为框架和工具提供信息

```cs
[Serializable] // 告诉序列化框架：这个类可以序列化
public class UserData { }

[Test] // 告诉测试框架：这是一个测试方法
public void Should_Calculate_Sum() { }

[HttpPost] // 告诉Web框架：这个方法处理POST请求
public IActionResult CreateUser() { }
```

运行时决策
```cs
// 安全检查器：通过读取元数据来决定访问权限
public class SecurityManager
{
    public bool CanAccess(object accessingObject, object targetObject)
    {
        // 读取FriendOf元数据
        var friendAttributes = targetObject.GetType()
            .GetCustomAttributes(typeof(FriendOfAttribute), false);
        
        // 如果 accessingObject 的类型在朋友列表中，允许访问
        return friendAttributes.Any(attr => 
            ((FriendOfAttribute)attr).Type == accessingObject.GetType());
    }
}
```

代码分析和工作流
```cs
[Obsolete("Use NewMethod instead")] // 编译器看到这个会显示警告
public void OldMethod() { }

[Conditional("DEBUG")] // 只在DEBUG模式下编译这个方法
public void DebugOnlyMethod() { }
```
Obsolete倒是见到过，是指旧代码未来要被舍弃了但我们姑且还能用一用，建议你把旧代码改了。
