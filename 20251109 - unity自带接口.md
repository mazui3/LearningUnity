问了下deepseek常见unity接口。

\
## 核心生命周期与引擎交互接口

**IEnumerator**（这个也是接口吗）\
用途： 支持迭代器和方法协程（Coroutine）的核心接口。\
必须实现的方法： MoveNext(), Reset(), Current。\
如何使用： 你通常通过返回类型为 IEnumerator 且包含 yield return 语句的方法来隐式实现它。
```cs
IEnumerator MyCoroutine() {
    yield return new WaitForSeconds(1);
    Debug.Log("1秒后执行");
}
// 使用：StartCoroutine(MyCoroutine());
```

**IEquatable<T>**\
用途： 为自定义结构或类提供强类型的相等性比较方法，比使用 object.Equals 更高效，能避免装箱。\
必须实现的方法： bool Equals(T other)。\
常见用途： 自定义 Vector3、Color 等值类型的比较。

**IDisposable**\
用途： 如前一个问题所述，用于释放非托管资源。\
必须实现的方法： void Dispose()。\
Unity 中的应用： Unity.Collections.NativeArray<T> 等。

\
## 物理系统相关接口

**ICollisionHandler**\
**IJobEntity**

\
## UI 系统相关接口

这些接口是 Unity UI（uGUI）系统的核心，用于处理交互。(的确，见的比较多）

**IPointerClickHandler**\
用途： 接收 UI 元素的点击（鼠标、触摸）事件。\
必须实现的方法： void OnPointerClick(PointerEventData eventData)。

**IPointerEnterHandler / IPointerExitHandler**\
用途： 接收鼠标/指针进入和离开 UI 元素的事件。\
必须实现的方法： void OnPointerEnter(PointerEventData eventData) / void OnPointerExit(PointerEventData eventData)。

**IDragHandler / IBeginDragHandler / IEndDragHandler**\
用途： 接收 UI 元素的拖拽事件。\
必须实现的方法： 对应的方法如 void OnDrag(PointerEventData eventData)。

**IScrollHandler**\
用途： 接收在 UI 元素上的滚动事件。\
必须实现的方法： void OnScroll(PointerEventData eventData)。

\
## 编辑器与序列化相关接口

**ISerializationCallbackReceiver**\
用途： 在 Unity 序列化（保存场景/预制体）或反序列化（加载场景/预制体）你的对象之前和之后，提供钩子函数以执行自定义逻辑。\
必须实现的方法： void OnBeforeSerialize(), void OnAfterDeserialize()。\
常见用途： 在保存前对复杂的数据结构进行整理，或在加载后重新初始化基于序列化数据的运行时状态。

**IExposedPropertyTable**\
用途： 主要用于 Visual Effect Graph 和 Shader Graph，用于管理“暴露的属性”的引用表。

\
## 高级编程与架构接口

**IComparable<T> / IComparable**\
用途： 允许对象实例之间进行排序比较。\
必须实现的方法： int CompareTo(T other)。\
常见用途： 在自定义集合中使用 List<T>.Sort()。

**IEnumerable<T> / IEnumerable**\
用途： 公开枚举数，该枚举数支持在集合上进行简单迭代。是 foreach 循环能够工作的基础。\
必须实现的方法： IEnumerator<T> GetEnumerator()。
